import * as PopoverPrimitive from '@radix-ui/react-popover'
import {styled} from '../../theme'

const rootPopoverStyles = styled(PopoverPrimitive.Root, {})

const triggerPopoverStyles = styled(PopoverPrimitive.Trigger, {})

const anchorPopoverStyles = styled(PopoverPrimitive.Anchor, {})

const contentPopoverStyles = styled(PopoverPrimitive.Content, {})

const closePopoverStyles = styled(PopoverPrimitive.Close, {})

const arrowPopoverStyles = styled(PopoverPrimitive.Arrow, {})

const portalPopoverStyles = styled(PopoverPrimitive.Portal, {})

export const StyledPopover = rootPopoverStyles
export const StyledTrigger = triggerPopoverStyles
export const StyledAnchor = anchorPopoverStyles
export const StyledContent = contentPopoverStyles
export const StyledClose = closePopoverStyles
export const StyledArrow = arrowPopoverStyles
export const StyledPortal = portalPopoverStyles
