import * as Slider from '@radix-ui/react-slider'
import {styled} from '../../theme'

const sliderRootStyles = styled(Slider.Root, {})

const sliderTrackStyles = styled(Slider.Track, {})

const sliderRangeStyles = styled(Slider.Range, {})

const sliderThumbStyles = styled(Slider.Thumb, {})

export const StyledSliderRoot = sliderRootStyles
export const StyledSliderTrack = sliderTrackStyles
export const StyledSliderRange = sliderRangeStyles
export const StyledSliderThumb = sliderThumbStyles

// Path: src/primitives/slider/slider.tsx
